import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  inputWrapper: {
    position: 'relative',
  },
  textInput: {
    paddingRight: 40,
  },
  sendIcon: {
    position: 'absolute',
    right: 0,
    bottom: 0,
  },
});

export default styles;
